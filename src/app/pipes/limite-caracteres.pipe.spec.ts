import { LimiteCaracteresPipe } from './limite-caracteres.pipe';

describe('LimiteCaracteresPipe', () => {
  it('create an instance', () => {
    const pipe = new LimiteCaracteresPipe();
    expect(pipe).toBeTruthy();
  });
});
